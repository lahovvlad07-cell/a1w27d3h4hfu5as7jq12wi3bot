"""
Конфигурация бота.

По твоей просьбе токен оставлен рабочим прямо здесь, чтобы можно было
сразу протестировать. Когда закончишь разработку — обязательно:
  1. Зайди к @BotFather -> /revoke -> получи новый токен.
  2. Впиши новый токен в .env (см. .env.example) вместо этой константы,
     либо просто замени значение по умолчанию ниже на новое.
  3. Убедись, что .env добавлен в .gitignore (уже добавлен в этом проекте),
     чтобы токен больше никогда не попадал в git/чаты/скриншоты.

Код уже поддерживает переменную окружения BOT_TOKEN "из коробки" —
если она задана (например, через .env и python-dotenv), возьмётся она,
иначе используется значение по умолчанию ниже.
"""
import os

try:
    from dotenv import load_dotenv
    load_dotenv()
except ImportError:
    # python-dotenv не обязателен для работы, если .env не используется
    pass

# TODO: отозвать и заменить перед публичным запуском (см. подсказку выше)
DEFAULT_TOKEN = "8708842706:AAHi5Rdb8kPxKGXMu1Knavf1ilowUxtfsrM"

BOT_TOKEN = os.getenv("BOT_TOKEN", DEFAULT_TOKEN)
SHOP_URL = os.getenv("SHOP_URL", "a1w27d3h4hfu5as7jq12wi3.vercel.app")
